# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
.eggs/
.kilo

# Data — raw/processed/external data stays local
/data/raw/
/data/processed/
/data/external/
/data/*.tar.gz
/data/*.zip
/data/*.pt
/data/*.json

# Experiment outputs (track plots, results & scripts; ignore heavy weights & DBs)
/experiments/checkpoints/*.pt
!/experiments/checkpoints/.gitkeep
/experiments/runs/*
!/experiments/runs/.gitkeep
# Regenerable binary arrays (feature/probability caches) — scripts rebuild them
/experiments/results/**/*.npz
# Regenerable model weights (meta-model state dicts) — scripts rebuild them
/experiments/results/**/*.pt
/experiments/tb_logs/
/experiments/*.db
/experiments/*.sqlite

# Virtual environment
.venv/
venv/
env/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Logs
logs/
!logs/.gitkeep

# Environment
.env
.env.*
*.tar.gz

# Godot 4
.godot/
godot/.godot/
*.uid

# Scratch
scratch/

# Tracked model weights — scripts rebuild them
experiments/checkpoints/*.zip
experiments/checkpoints/*.pt

# Training run outputs (logs, checkpoints, tensorboard, metrics)
src/training/logs/
experiments/runs/*

# Experiment metrics / consolidated outputs
experiments/metrics/*
!experiments/metrics/.gitkeep

# Regenerable binary arrays & cached features (scripts rebuild them)
**/*.npz

# Generated models / weights — never committed
experiments/results/**/*.pt
experiments/results/**/*.onnx
experiments/results/**/*.ckpt

# Database artifacts
experiments/*.db
experiments/*.sqlite
